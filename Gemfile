# frozen_string_literal: true

source 'https://rubygems.org'

repo_name = 'TDD-Project'

git_source(:github) {|repo_name| 'https://github.com/#{repo_name}' }

gem 'rubocop', '>= 1.0', '< 2.0'

# gem "rails"
